// Static SPA: prerender the shell, render entirely client-side (WASM + Worker
// + Three.js need the browser). adapter-static turns this into index.html.
export const prerender = true;
export const ssr = false;
